{
  "name": "@ci-flake-lab/node-test",
  "private": true,
  "scripts": {
    "test:spec": "node --test --test-reporter=spec test/*.test.js",
    "test:tap": "node --test --test-reporter=tap test/*.test.js"
  }
}
